<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Recuperar contraseña | TaskFlow</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen">

  <main class="mx-auto max-w-6xl px-4 py-12">
    <div class="mx-auto max-w-md">
      <h1 class="text-3xl font-bold text-center mb-4">Recuperar contraseña</h1>
      <p class="text-center text-sm text-gray-600 mb-6">
        Ingresa tu correo y te enviaremos un link para restablecer tu contraseña.
      </p>

      <form id="forgotForm" class="space-y-4 rounded-lg border bg-white p-6 shadow-sm">
        <div class="space-y-1">
          <label class="text-sm text-gray-700">Correo</label>
          <input id="email" type="email" class="w-full h-10 rounded-md border px-3 text-sm" placeholder="usuario@ejemplo.com" required>
        </div>

        <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-md text-sm">
          Enviar enlace
        </button>

        <p id="msg" class="mt-2 text-center text-sm text-red-600 hidden"></p>

        <div class="pt-2 text-center text-sm">
          <a href="/login" class="text-blue-600 underline underline-offset-4">Volver al inicio de sesión</a>
        </div>
      </form>
    </div>
  </main>

  <script>
    const form = document.getElementById("forgotForm");
    const msg = document.getElementById("msg");

    form.addEventListener("submit", async (e) => {
      e.preventDefault();
      msg.classList.add("hidden");

      const email = document.getElementById("email").value.trim();

      try {
        const res = await fetch("/auth/forgot-password", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ email }),
        });

        if (!res.ok) {
          const data = await res.json();
          throw new Error(data.message || "Error al enviar correo");
        }

        msg.textContent = "Revisa tu correo 📧";
        msg.classList.remove("text-red-600");
        msg.classList.add("text-green-600");
        msg.classList.remove("hidden");

      } catch (err) {
        msg.textContent = err.message;
        msg.classList.remove("hidden");
      }
    });
  </script>

</body>
</html>
